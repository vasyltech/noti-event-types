{
    "Event": "action:deleted_plugin",
    "Level": "critical",
    "RequiredVersion": "WordPress 4.4.0+",
    "Listener": [
        {
            "Event": "action:delete_plugin",
            "Data": {
                "plugin": "${FUNC.get_plugin_data(ARGS.0).Name}"
            }
        }
    ],
    "Metadata": {
        "user_id": "${USER.ID}",
        "user_ip": "${USER.ip}",
        "plugin": "${LISTENER.0.plugin}"
    },
    "LogMessageMarkdown": "**${FUNC.get_userdata(META.user_id).display_name}** deleted the **${META.plugin}** plugin",
    "Notifications": [
        {
            "Type": "email"
        }
    ]
}